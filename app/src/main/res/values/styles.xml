<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="fontFamily">@font/iran_marker</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:soundEffectsEnabled">false</item>

    </style>

    <style name="DefaultButton" parent="AppTheme">
        <item name="android:padding">12dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="GlowTextView" parent="AppTheme">
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:shadowColor">@color/colorPrimary</item>
        <item name="android:shadowRadius">15</item>
        <item name="android:shadowDx">0</item>
        <item name="android:shadowDy">0</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="PrimaryButton" parent="AppTheme">
        <item name="android:background">@drawable/btn_main_parent</item>
        <item name="android:layout_margin">16dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
